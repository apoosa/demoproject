pipeline {
    agent any
    triggers {
         cron('*0/1 * * * *')
     }
     stages {
        stage("Hello") {
            steps {
                echo "Hello World"
            }
        }
    }
}
